# T19 · 任务/目标状态机 + 断点续跑 + 优先级仲裁

- 里程碑：M2.8
- 依赖：T09、T17
- PRD 对应：FR-8.4、§6.5 Task/Goal
- 状态：⬜ 未开始

## 目标
管理长时目标与待办：常开 Agent 可能被打断/切源/重启，任务状态要能**恢复（断点续跑）**；并对并发目标做优先级仲裁。

## 交付物
- [ ] `src/opensensorium/core/task_goal.py`：任务/目标状态机（待办→进行→阻塞→完成/失败）。
- [ ] 断点续跑：状态持久化（与 T17 情景记忆/存储共用），重启/被打断后可恢复到断点。
- [ ] 优先级仲裁：并发目标按优先级 + 显著性排程。
- [ ] 测试：`tests/test_task_goal.py`。

## 实现要点
- 与 barge-in（T11/FR-2.4）协作：被打断的任务不丢，转阻塞态待恢复。
- 与调度（T18）、显著性（T12）共同决定"此刻做什么"。

## 分工（Agent 做什么 / 人做什么）
- **Agent**：实现任务/目标状态机、断点续跑、优先级仲裁 + 测试。
- **人**：实测多步任务中途打断/切源/重启后能续跑完成。

## 验收标准（可验证）
- [ ] `tests/test_task_goal.py`：任务状态迁移正确；模拟"重启"后从断点恢复；两个并发目标按优先级选中。
- [ ] 集成：给一个多步任务，中途打断/切源后能续跑完成。

## Git
- 分支：`feat/t19-task-goal`
- 提交：`git commit -m "T19: 任务/目标状态机 + 断点续跑 + 优先级"`
- 完成后改状态 ✅。
