# T18 · 调度：cron/间隔/一次性/条件触发 → 内部事件

- 里程碑：M2.8
- 依赖：T09
- PRD 对应：FR-8.1–8.3、§3.7 时间触发
- 状态：⬜ 未开始

## 目标
主动性的"时间"那一维（OpenClaw 式定时任务接入点）。到点/命中作为**内部事件**进认知核，与感知/channel 事件统一仲裁。

## 交付物
- [ ] `src/opensensorium/scheduler/scheduler.py`：定时任务——cron / 固定间隔 / 一次性提醒（"每天9点汇报""10分钟后提醒我"）。
- [ ] `src/opensensorium/scheduler/conditional.py`：条件触发——"当感知到 X → 做 Y"（如"看到构建失败就通知我"）。
- [ ] 触发即产生**内部事件**发上 T04 总线，进认知核仲裁（FR-8.3、§3.7）。
- [ ] 任务持久化（重启后定时任务仍在）。
- [ ] 测试：`tests/test_scheduler.py`。

## 实现要点
- 三类触发源（感知/时间/事件）统一进认知核由显著性/优先级仲裁"此刻做什么"（§3.7）——本任务提供"时间"与"条件"两类。
- 条件触发要订阅感知总线做匹配。

## 分工（Agent 做什么 / 人做什么）
- **Agent**：实现定时/条件调度、内部事件、持久化 + 测试。
- **人**：实测"10 分钟后提醒""看到构建失败就通知"、重启后任务仍在。

## 验收标准（可验证）
- [ ] `tests/test_scheduler.py`：cron/间隔/一次性到点触发内部事件；条件规则命中感知即触发；重启后任务恢复。
- [ ] 集成："10 分钟后提醒我"到点主动开口；"看到构建失败就通知我"在注入失败画面时触发。

## Git
- 分支：`feat/t18-scheduler`
- 提交：`git commit -m "T18: 调度（cron/间隔/一次性/条件触发）"`
- 完成后改状态 ✅。
